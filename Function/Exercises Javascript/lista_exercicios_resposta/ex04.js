function valoresdiv(dividendo, divisor){
    resultado = Math.floor(dividendo/divisor)
    resto = dividendo%divisor
    console.log(`valor do resultado é: ${resultado}`)
    console.log(`Valor do resto é: ${resto}`)
}

valoresdiv(5,2)
